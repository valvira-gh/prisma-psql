import styles from "./page.module.css";

const Home: React.FC = () => {
  // code

  return (
    <main className={styles.main}>
      <h1 className={styles.title}>Prisma - PostgreSQL</h1>
    </main>
  );
};

export default Home;
